apiVersion: v1
kind: Service
metadata:
  name: ms-catalog
  labels:
    app: ms-catalog
spec:
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: ms-catalog
